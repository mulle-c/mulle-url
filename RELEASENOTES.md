### 2.3.12






* treat `MULLE__CORE_BUILD` the same as `MULLE__URL_BUILD` so symbols are exported when mulle-url is built as part of mulle-core
* correct dynamic/static include macro names to ensure proper extern/global visibility `(MULLE__URL_INCLUDE_DYNAMIC` / `MULLE__URL_INCLUDE_STATIC)`
